; Exercise 4.66

; Ben realized that if the query pattern is satisfied by multiple items, it
; will add them all up into his accumulation function.

; In order to note run into this situation, he could filter the stream using
; (not (same ?x ?y)) in order to ensure uniqueness of query pattern result.
